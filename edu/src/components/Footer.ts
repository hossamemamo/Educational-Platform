import { SxProps, Theme } from "@mui/material";



export const BoxStyles: Record<string, SxProps<Theme> | undefined> = {
    sx: {
        width: '100%',
        height: 360,
        position: 'relative',
        bottom: 0,
        backgroundColor: 'primary.white',
      }

}